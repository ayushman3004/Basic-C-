#include<iostream>
using namespace std;
int main(){
  int n;
  cout<<"enter your number"<<endl;
  cin>>n;

  // (condition) ? if ture : if false ;

  (n%2==0) ? cout<<"even" : cout<<"odd";
    return 0;
}